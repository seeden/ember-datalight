var should = require('should');

describe('Base Wrapper', function() {

	it('should be able to create', function(done) {

	/*
		user.number().should.equal(8888);

		user.super().should.equal('super');

		user.super2().should.equal('super2');*/

		done();
	});	
});